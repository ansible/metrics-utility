---
- name: "Role task — debug from role"
  ansible.builtin.debug:
    msg: "Running inside an included role"

- name: "Role task — command from role"
  ansible.builtin.command: echo "role task done"

- name: "Role task — failure in role (ignored)"
  ansible.builtin.command: /bin/false
  ignore_errors: true
