# Build / Reports

The `build_report` subcommand reads gather tarballs for a given time interval and produces XLSX reports.

Report types:
- **CCSP** / **CCSPv2** - the main billing reports
- **RENEWAL_GUIDANCE** - reads directly from the DB rather than from tarballs
